
# 根目录
cd D:\sichaun-travel-system
# 登录 MySQL
mysql -u root -p
mysql8
# 执行初始化脚本
source database/init.sql;
EXIT

# 创建后端项目
cd D:\sichaun-travel-system\server
npm init -y
npm install express cors mysql2 bcryptjs jsonwebtoken axios cheerio node-cron
npm install nodemon --save-dev


# 终端 1 — 启动后端
npx nodemon app.js
# 服务运行于 http://localhost:3000





# 创建前端项目
# npm create vite@latest client -- --template vue
cd D:\sichaun-travel-system\client
npm install
npm install vue-router@4 pinia axios element-plus @amap/amap-jsapi-loader
//npm install element-plus @element-plus/icons-vue
# 终端 2 — 启动前端
npm run dev
# 访问 http://localhost:5173



# 终端 3 — 启动爬虫定时任务（可选）
cd server
node -e "require('./crawler/scheduler').start()"


# 需求分析
https://github.com/tongdi0918/shu_you.git现在重新对我的项目进行修改.
每个页面顶部都务必保证保持首页的原样导航条样式与布局。因为首页已经有了导航条就不必再添加了，避免首页形成2个一模一样的导航条。
首页中的景区卡片组添加的“刷新”按钮要实现更新景区卡片的功能(当前没有实现)；同样美食卡片组也要添加“刷新”按钮以实现更新美食卡片功能(当前也没有实现)。我数据库里那么多记录，要随机展示噻。
”路程规划“页面中输入了出发地与目的地后“高德地图驾车路线图”中的2个地点的线路轨迹要标识出来，类似于高德地图的导航功能。这个必须解决

个人主页增加“当前位置”字段(并且数据库也增加“用户位置”字段)。再者“智能推荐”页面要对城市进行分类展示景区方块与美食方块，使用户选中欲去的景区与美食，再选择当前位置(并同步到个人主页“当前位置”)，进行合理规划（要求同一个城市的景区与美食要求排列在一起）。注意是根据用户的选择与当前位置进行规划。

强调一下，要根据我的的所有实际完整项目文件进行修改，务必不改变原有的界面与功能，你只能添加新的功能，原有的功能不能缺省，也不能缺省原有的页面跳转逻辑！！！实事求是，也不要你认为什么就省略代码；再次强调我要所有的和完整的代码替换文件及其文件位置，还要给出项目的目录结构及详细的操作步骤。

# 待实现功能
爬取抖音三五个景区与美食评论
天气预警
客户端可视化展示要有用途